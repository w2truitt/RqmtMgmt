apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rqmtmgmt-ingress
  namespace: rqmtmgmt
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: "web,websecure"
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/redirect-scheme: "https"
spec:
  ingressClassName: "traefik"
  tls:
  - hosts:
    - rqmtmgmt.local
    secretName: rqmtmgmt-tls
  rules:
  - host: rqmtmgmt.local
    http:
      paths:
      # IdentityServer endpoints - MUST be first for proper routing
      - path: /.well-known
        pathType: Prefix
        backend:
          service:
            name: identityserver-service
            port:
              number: 80
      - path: /connect
        pathType: Prefix
        backend:
          service:
            name: identityserver-service
            port:
              number: 80
      - path: /Account
        pathType: Prefix
        backend:
          service:
            name: identityserver-service
            port:
              number: 80
      # API routes - backend service
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
      # Swagger documentation - backend service
      - path: /swagger
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
      # Health checks - backend service
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
      # Frontend - catch all other routes (MUST be last)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
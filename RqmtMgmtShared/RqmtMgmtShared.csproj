<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Version>1.0.43</Version>
    <PackageVersion>1.0.43</PackageVersion>
    <PackageReleaseNotes>Enforce proper section hierarchy: Added required ProjectId to DocumentSectionDto and required SectionId to RequirementDto. All sections must belong to a project, all requirements must belong to a section.</PackageReleaseNotes>
  </PropertyGroup>

</Project>